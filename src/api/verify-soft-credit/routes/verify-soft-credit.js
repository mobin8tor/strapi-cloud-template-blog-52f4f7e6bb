'use strict';

/**
 * verify-soft-credit router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::verify-soft-credit.verify-soft-credit');
