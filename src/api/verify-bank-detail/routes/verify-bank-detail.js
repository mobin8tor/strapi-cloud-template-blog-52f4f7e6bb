'use strict';

/**
 * verify-bank-detail router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::verify-bank-detail.verify-bank-detail');
