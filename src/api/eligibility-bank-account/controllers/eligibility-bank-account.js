'use strict';

/**
 * eligibility-bank-account controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::eligibility-bank-account.eligibility-bank-account');
