'use strict';

/**
 * eligibility-bank-account router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::eligibility-bank-account.eligibility-bank-account');
