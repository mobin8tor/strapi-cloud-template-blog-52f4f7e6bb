'use strict';

/**
 * application-resume router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::application-resume.application-resume');
