'use strict';

/**
 * eligibility-work-auth router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::eligibility-work-auth.eligibility-work-auth');
