'use strict';

/**
 * eligibility-work-auth service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::eligibility-work-auth.eligibility-work-auth');
