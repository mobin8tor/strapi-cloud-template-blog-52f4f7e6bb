'use strict';

/**
 * verify-offer-select controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::verify-offer-select.verify-offer-select');
